import { HeaderContainer } from "../../styles/public/Header";

export const HeaderWaveSection = ({ children }: { children: React.ReactNode }) => (
    <HeaderContainer>
        {children}
    </HeaderContainer>
);
